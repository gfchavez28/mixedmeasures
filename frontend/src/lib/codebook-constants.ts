/** Shared color constants for Codebook view components (SVG rendering). */

/** Default fallback for codes/categories with no assigned color. */
export const COLOR_DEFAULT = '#6b7280'

/** Selection/focus indicator (blue-500). */
export const COLOR_SELECT = '#3b82f6'

/** Universal code indicator (emerald-500). */
export const COLOR_UNIVERSAL = '#10b981'

/** Universal code text label (emerald-700). */
export const COLOR_UNIVERSAL_TEXT = '#059669'

/** Creation preview spotlight indicator (green-500). */
export const COLOR_SPOTLIGHT = '#22c55e'

/** Theme-aware surface colors for opaque SVG backgrounds. */
export const SURFACE_LIGHT = '#fcfcfb'
export const SURFACE_DARK = '#1f2329'
