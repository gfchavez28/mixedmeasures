fastapi==0.136.1
starlette==1.6.0  # security floor: GHSA-7f5h-v6xp-fcq8 (Range-header parsing DoS in FileResponse), transitive via fastapi — 1.6.0 is far above it. Range streaming is load-bearing for media seek (audio + video V1).
                  # EXACT since 2026-08-27 (#843), and the reason is reproducibility, same as the numbers stack below. This was the LAST unbounded specifier in this file, so a
                  # fresh resolve took whatever PyPI offered that day while the dev venv sat on 0.52.1 (2026-01-18): v1.3.0, v1.3.1 and v1.3.2 were every one of them frozen
                  # against a starlette MAJOR that had never executed on the developer's machine, and two builds from one source tag could ship different majors. 1.6.0 is what
                  # CI has been running green and what those three releases actually shipped, so this pin adopts the shipped version rather than reverting to the stale local one.
                  # Soaked locally at 1.6.0 before pinning: 2837 passed, 0 skipped, MM_REQUIRE_R=1 — identical to the pre-bump baseline.
                  # Bump DELIBERATELY and re-run the suite locally FIRST: app/main.py imports BaseHTTPMiddleware directly (the base of BOTH SecurityHeadersMiddleware and
                  # LoopbackTokenMiddleware) and JSONResponse, whose allow_nan=False rendering is a load-bearing invariant (#689/#566) — assert it still RAISES on nan.
uvicorn[standard]==0.42.0
pydantic==2.13.3
pydantic-settings==2.14.2  # security floor: GHSA-4xgf-cpjx-pc3j (NestedSecretsSettingsSource symlink traversal; MM doesn't use secrets_dir). Bumped from 2.14.0.
sqlalchemy==2.0.49
alembic>=1.13.0,<2.0.0
bcrypt==5.0.0
python-multipart==0.0.31  # security floor: CVE-2026-53538/53539/53540 (multipart parser DoS); FastAPI form/upload parsing. Bumped from 0.0.27.
openpyxl==3.1.5
# ── The numbers stack: EXACT pins, and the reason is reproducibility, not security ──
# A range here means the installer ships whatever the build machine resolved that
# day, so two builds from identical source can compute different statistics. For a
# tool whose proposition is numbers a researcher can stand behind, that is a defect.
#
# It is not hypothetical: `scipy>=1.10.0,<2.0.0` had CI on 1.18.0 (published
# 2026-06-19) while the dev venv sat on 1.17.0, and the two disagree about
# `mannwhitneyu` on all-tied input — 1.17 returns p=1.0, 1.18 returns nan (the more
# honest answer: with no variance in the ranks there is no test). That divergence
# turned CI red for five consecutive pushes while every local run reported green.
#
# Bump these DELIBERATELY, together, and re-run the R round-trip oracles
# (`test_export_r_roundtrip.py`) — they are what prove the numbers did not move.
numpy==2.5.2        # DIRECT dependency: imported by comparisons/correlations/
                    # cross_tabulation/data_quality/statistical_tests. It was never
                    # declared and arrived transitively via scipy/statsmodels — an
                    # undeclared contract that a scipy release could have broken.
scipy==1.18.0
statsmodels==0.14.6
python-docx>=1.1.0,<2.0.0
lxml==6.1.0  # security floor: CVE-2026-41066, transitive via python-docx (uploaded .docx parsing). Pinned to vetted 6.1.0; revisit when 6.1.x clears cooldown.
pdfminer.six==20260107  # PDF text extraction (replaced AGPL-licensed PyMuPDF for Apache-2.0 compat, 2026-06-01). Pulls cryptography (Apache/BSD) + cffi (MIT) transitively.
tinytag==2.2.1  # audio duration/VBR metadata (replaced GPLv2+ mutagen for Apache-2.0 compat, 2026-06-01). Zero runtime deps.
pyreadstat==1.3.5  # SPSS .sav dataset import (#28). Apache-2.0, wrapping ReadStat (MIT) — permissive. Pulls narwhals (MIT) + numpy (already direct). Lazy-imported inside services/sav_import.py; read with output_format="dict" so pandas (an installed statsmodels transitive) is never imported. Ships a Cython extension — PyInstaller must collect it (see mixedmeasures.spec).
defusedxml>=0.7.0,<1.0.0
slowapi>=0.1.9,<1.0.0
sqlcipher3==0.6.2  # SQLCipher 4.x at-rest DB encryption (packaging P4; default-OFF via mm_encryption_enabled — dev.db + the test suite stay plaintext). Plain `sqlcipher3`, NOT `sqlcipher3-binary` (that one is Linux-x86_64-only + SQLCipher 3.x). 0.6.2 ships prebuilt wheels for win_amd64/win32/win_arm64 + macOS + manylinux/musllinux, bundling SQLCipher 4.12.0 statically. Exact-pin keeps the bundled SQLCipher version identical across OS wheels (no cipher_compatibility juggling). PyInstaller must collect the bundled extension (Phase 7).
